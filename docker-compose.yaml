services:
  playwright-test:
    image: playwright-test
    build: 
      context: .
      dockerfile: ./dockerfile
    command: npm run testUsePageObjects-chrome
    volumes:
      - ./playwright-report/:/app/playwright-report
      - ./test-results/:/app/test-results